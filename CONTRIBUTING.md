# Contributing

Thanks for improving ContextWord. Please create a focused branch, run `npm run lint`, `npm run test`, and `npm run build`, then describe the user-visible change in your pull request.

Never add analytics, transmit page text, or introduce new extension permissions without documenting the privacy and security impact in the README.
